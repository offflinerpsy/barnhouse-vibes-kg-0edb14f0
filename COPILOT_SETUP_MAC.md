# 🍎 Настройка Copilot для ERA Concept Home (macOS)

> Инструкция для AI-агента на MacBook

---

## 🚀 Быстрый старт

### 1. Скачать инструкции из GitHub

```bash
# Перейти в папку проекта
cd ~/path/to/barnhouse-vibes-kg

# Скопировать инструкции в глобальную папку aitk
mkdir -p ~/.aitk/instructions
cp .github/copilot-instructions.md ~/.aitk/instructions/era-concept.instructions.md

echo "✅ Инструкции установлены!"
```

### 2. Настроить GitHub токен

```bash
# Сохранить токен в файл (получи токен у @offflinerpsy)
echo "ghp_ваш_токен_здесь" > ~/.github_token
chmod 600 ~/.github_token

# Проверить
cat ~/.github_token
```

### 3. Загрузить токен в сессию

```bash
# Добавить в ~/.zshrc или ~/.bashrc для автозагрузки
echo 'export GH_TOKEN=$(cat ~/.github_token)' >> ~/.zshrc

# Или загрузить вручную для текущей сессии
export GH_TOKEN=$(cat ~/.github_token)

# Проверить
gh auth status
```

### 4. Проверить работу

Открой проект в VS Code и отправь тестовое сообщение агенту:
```
тестовое сообщение
```

Агент должен ответить что загрузил Memory Graph и знает про:
- Репозиторий: `barnhouse-vibes-kg-0edb14f0`
- Данные: `src/data/catalog-models.ts`
- Сервер: `185.196.117.49` → `/var/www/era-concept/`

---

## 📂 Структура файлов

```
~/.aitk/instructions/
└── era-concept.instructions.md  ← Инструкции для проекта ERA

~/.github_token                  ← GitHub токен (секрет!)

~/path/to/barnhouse-vibes-kg/
├── .github/
│   └── copilot-instructions.md  ← Исходник инструкций (в репо)
├── ARCHITECTURE.md              ← Архитектура проекта
├── DESIGN_SYSTEM.md             ← Дизайн-система
├── CATALOG_GUIDE.md             ← Гайд по моделям
└── src/
    └── data/
        └── catalog-models.ts    ← Данные всех моделей
```

---

## 🔄 Синхронизация инструкций

Если инструкции обновились в репо — синхронизируй их:

```bash
cd ~/path/to/barnhouse-vibes-kg
git pull origin main
cp .github/copilot-instructions.md ~/.aitk/instructions/era-concept.instructions.md
echo "✅ Инструкции обновлены!"
```

---

## 🛠 Команды для работы (macOS)

### Git и GitHub

```bash
# Загрузить токен
export GH_TOKEN=$(cat ~/.github_token)

# Проверить issues
gh issue list --state open

# Создать issue
gh issue create --title "BUG: описание" --body "Детали"

# Закрыть issue
gh issue close 5 --comment "Решено в коммите abc123"
```

### Сборка и деплой

```bash
# Сборка
npm run build

# Деплой через rsync (SSH ключ должен быть настроен)
rsync -avz --delete dist/ root@185.196.117.49:/var/www/era-concept/

# Или через scp (по файлам)
scp dist/index.html root@185.196.117.49:/var/www/era-concept/
scp dist/assets/index-*.js dist/assets/index-*.css root@185.196.117.49:/var/www/era-concept/assets/
```

### SSH (если нужно зайти на сервер)

```bash
ssh root@185.196.117.49
```

---

## ⚠️ Важные отличия от Windows

| Действие | Windows (PowerShell) | macOS (Zsh/Bash) |
|----------|---------------------|------------------|
| Загрузить токен | `$env:GH_TOKEN = Get-Content ...` | `export GH_TOKEN=$(cat ...)` |
| Путь к токену | `$env:USERPROFILE\.github_token` | `~/.github_token` |
| Путь к aitk | `$env:USERPROFILE\.aitk\instructions\` | `~/.aitk/instructions/` |
| Копировать файл | `Copy-Item ... -Force` | `cp ...` |
| Создать папку | `New-Item -ItemType Directory` | `mkdir -p` |

---

## 📖 Документация проекта

После настройки изучи эти файлы в репо:

1. [ARCHITECTURE.md](ARCHITECTURE.md) — архитектура проекта
2. [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) — цвета, типографика
3. [CATALOG_GUIDE.md](CATALOG_GUIDE.md) — гайд по моделям домов

---

## 🆘 Проблемы?

### Агент не читает инструкции
```bash
# Проверить что файл существует
ls -la ~/.aitk/instructions/era-concept.instructions.md

# Проверить содержимое (первые 10 строк)
head -10 ~/.aitk/instructions/era-concept.instructions.md

# Перезагрузить VS Code
# Cmd+Shift+P → "Developer: Reload Window"
```

### GitHub токен не работает
```bash
# Проверить что токен загружен
echo $GH_TOKEN

# Проверить авторизацию
gh auth status

# Если не работает — заново загрузить
export GH_TOKEN=$(cat ~/.github_token)
```

### Не может деплоить
```bash
# Проверить SSH подключение
ssh root@185.196.117.49 "echo OK"

# Если просит пароль — настроить SSH ключ
# (попроси @offflinerpsy добавить твой публичный ключ на сервер)
```

---

## ✅ Чеклист готовности

- [ ] Клонирован репозиторий `barnhouse-vibes-kg-0edb14f0`
- [ ] Установлены зависимости (`npm install`)
- [ ] Инструкции скопированы в `~/.aitk/instructions/era-concept.instructions.md`
- [ ] GitHub токен сохранён в `~/.github_token`
- [ ] Токен загружен: `export GH_TOKEN=$(cat ~/.github_token)`
- [ ] `gh auth status` показывает "Logged in"
- [ ] Тестовое сообщение агенту работает (читает Memory Graph)
- [ ] SSH подключение к серверу работает (опционально)

**Готов к работе!** 🎉

---

*При вопросах пиши @offflinerpsy или проверяй GitHub Issues*
