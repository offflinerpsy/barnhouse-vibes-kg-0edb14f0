# 🚨 GIT WORKFLOW — ERA Concept Home KG

> **КРИТИЧЕСКИ ВАЖНО ДЛЯ AI-АССИСТЕНТОВ И РАЗРАБОТЧИКОВ**

---

## ⛔ ВНИМАНИЕ! ЧИТАЙ ПЕРЕД ЛЮБЫМИ GIT ОПЕРАЦИЯМИ!

### ОСНОВНОЙ РЕПОЗИТОРИЙ (ЕДИНСТВЕННЫЙ ДЛЯ РАБОТЫ):

```
🟢 ИСПОЛЬЗУЕМ: https://github.com/offflinerpsy/barnhouse-vibes-kg-01b96b92
```

### СТАРЫЙ РЕПОЗИТОРИЙ (НЕ ИСПОЛЬЗОВАТЬ!):

```
🔴 НЕ ИСПОЛЬЗОВАТЬ: https://github.com/offflinerpsy/barnhouse-vibes-kg
```

---

## 📋 Почему так?

Проект синхронизируется с сервисом **Lovable.dev** для разработки UI.
Lovable создал свой репозиторий и работает ТОЛЬКО с ним.

**Воркфлоу:**
```
Локальная работа → commit → push (в Lovable репо) → Lovable видит изменения
                                    ↓
Lovable работа → push (в тот же репо) → git pull локально
```

---

## 🔧 Настройка remote (уже сделана)

```bash
# Текущее состояние:
origin      → https://github.com/offflinerpsy/barnhouse-vibes-kg-01b96b92.git  ✅ ОСНОВНОЙ
old-origin  → https://github.com/offflinerpsy/barnhouse-vibes-kg.git           ⚠️ БЭКАП
```

---

## 📝 Команды для работы

### Обычный workflow:

```bash
# Забрать изменения из Lovable
git pull origin main

# Запушить свои изменения
git add .
git commit -m "описание"
git push origin main
```

### Если AI спрашивает куда пушить:

```
ОТВЕТ: origin main
РЕПО: barnhouse-vibes-kg-01b96b92
```

---

## ⚠️ ЗАПРЕЩЕНО

- ❌ `git push old-origin` — НИКОГДА
- ❌ Менять origin на старый репо
- ❌ Пушить в `barnhouse-vibes-kg` (без суффикса)

---

## ✅ РАЗРЕШЕНО

- ✅ `git push origin main`
- ✅ `git pull origin main`
- ✅ Работа с `barnhouse-vibes-kg-01b96b92`

---

## 🔄 Если нужно синхронизировать бэкап

Только вручную и осознанно:

```bash
git push old-origin main  # Только для бэкапа!
```

---

*Последнее обновление: Январь 2026*
*Изменено: переход на Lovable репозиторий как основной*
