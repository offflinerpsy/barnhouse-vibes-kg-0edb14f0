---
description: ERA Concept Home KG - инструкции для AI-агента
applyTo: '**'
---

# ERA Concept Home KG — Инструкции для AI-агента

## 🔧 ОБЯЗАТЕЛЬНЫЕ ИНСТРУМЕНТЫ

При работе с проектом **ВСЕГДА** использовать:

| Инструмент | Когда использовать |
|------------|-------------------|
| `mcp_memory_read_graph` | **В начале сессии** — загрузить контекст проекта |
| `mcp_context7_get-library-docs` | Код с Framer Motion, Radix UI, Tailwind, react-pdf |
| `mcp_ddg-search_web-search` | Поиск решений багов, актуальная документация |
| `mcp_sequential-th_sequentialthinking` | Сложные задачи, рефакторинг, диагностика |

**Правило:** Если не уверен — используй Sequential Thinking. Лучше подумать чем сломать.

---

## 🔄 WORKFLOW

```
БАГ/ЗАДАЧА → ISSUE → FIX → COMMIT → PUSH → АВТОДЕПЛОЙ ✨ → CLOSE ISSUE
```

### 1. Получил задачу или нашёл баг
```bash
# Проверить открытые issues
gh issue list --state open

# Создать issue если его нет
gh issue create --title "BUG: описание" --body "Детали проблемы"
```

### 2. Работа над фиксом
```bash
git checkout main && git pull origin main
git checkout -b fix/issue-N-описание

# ... код ...
```

### 3. Коммит и пуш
```bash
git add <конкретные файлы>
git commit -m "fix(scope): описание. Closes #N"
git push origin fix/issue-N-описание
```

### 4. Merge → автодеплой
```bash
git checkout main
git merge fix/issue-N-описание
git push origin main

# 🎉 GitHub Actions автоматически:
# - npm ci
# - npm run build
# - rsync на сервер
# - chmod/chown
# Отследить: GitHub → Actions
```

### 5. Закрыть issue
```bash
gh issue close N --comment "Решено в коммите XYZ"
```

> ⚠️ **Ручной деплой больше НЕ НУЖЕН!** Actions делает всё сам.

---

## 🌐 PRODUCTION

```
Server:    185.196.117.49
Domain:    era-home.kg
Web Root:  /var/www/era-concept/   ← ВАЖНО! Не era-home.kg!
SSH:       ssh root@185.196.117.49
```

---

## 🔐 GITHUB

```
Repo: offflinerpsy/barnhouse-vibes-kg-0edb14f0
```

```powershell
# Загрузить токен (Windows)
$env:GH_TOKEN = Get-Content $env:USERPROFILE\.github_token

# Загрузить токен (Mac/Linux)
export GH_TOKEN=$(cat ~/.github_token)
```

---

## 📂 СТРУКТУРА ПРОЕКТА

```
src/
├── data/
│   └── catalog-models.ts      ← ВСЕ ДАННЫЕ МОДЕЛЕЙ ЗДЕСЬ
├── components/
│   └── landing/
│       ├── Catalog.tsx        ← Desktop + кнопка "Открыть каталог"
│       ├── CatalogAppView.tsx ← Mobile fullscreen overlay
│       ├── Hero.tsx
│       ├── Footer.tsx
│       └── ContactModal.tsx
└── index.css                  ← CSS переменные
```

---

## 🎨 ДИЗАЙН-СИСТЕМА

| Переменная | Значение | Использование |
|------------|----------|---------------|
| `--primary` / `--gold` | `hsl(32 42% 59%)` | Акцент, кнопки |
| `--background` | `hsl(38 25% 95%)` | Основной фон |
| `--charcoal` | `hsl(30 15% 20%)` | Hero, Footer |
| `--foreground` | `hsl(30 10% 15%)` | Текст |

**Шрифт:** Rising Sun (`/fonts/risingsun-*.woff2`)

```tsx
// ✅ Правильно
className="text-primary bg-background"
className="text-[hsl(var(--gold))]"

// ❌ Неправильно
className="text-[#C3996B]"  // Хардкод запрещён
```

---

## 🏠 КАТАЛОГ МОДЕЛЕЙ

**Файл данных:** `src/data/catalog-models.ts` → `CATALOG_MODELS[]`

**Структура папок:**
```
public/catalog/
├── covers/           ← Обложки моделей
└── model-[этажей]-[площадь]/
    ├── gallery/      ← Основные фото
    ├── gallery-extra/← Доп. фото
    └── floor-plan/   ← Планировки
```

**Именование:** Model 1, 2, 3, 4, 6, 8 (одноэтажные) | Model 2X, 4X, 7X, 12X (двухэтажные)

> ⚠️ Model 5 и Model 7 НЕ СУЩЕСТВУЮТ — это задокументировано

---

## 📐 БРЕЙКПОИНТЫ

```
sm:  640px   md:  768px   lg:  1024px   xl:  1280px   2xl: 1400px
```

---

## ⛔ НЕ ДЕЛАТЬ

1. **НЕ** хардкодить цвета — только CSS переменные
2. **НЕ** менять `src/components/ui/` без причины (shadcn)
3. **НЕ** менять `catalogPath` без переименования папок
4. **НЕ** коммитить без проверки (Actions соберёт автоматически)
5. **НЕ** деплоить вручную — GitHub Actions делает это сам
6. **НЕ** деплоить в `/var/www/era-home.kg/` — только `/var/www/era-concept/`
7. **НЕ** запускать `npm run dev` в терминале VS Code — сервер умрёт при закрытии терминала

---

## 🖥️ DEV СЕРВЕР

> ⚠️ **ВАЖНО:** Запускать dev сервер ТОЛЬКО в отдельном терминале Windows (PowerShell/Terminal), НЕ в VS Code!

```powershell
# В отдельном окне PowerShell (Win+X → Terminal)
cd "C:\Users\Makkaroshka\Desktop\era concept home KG\barnhouse-vibes-kg"
npm run dev
```

**Почему:** VS Code терминалы закрываются при переключении задач/панелей → сервер падает → бесит.

---

## 🔄 АКТУАЛИЗАЦИЯ ДОКУМЕНТАЦИИ — ОБЯЗАТЕЛЬНО!

**Если во время работы что-то изменилось — ОБНОВЛЯЙ ДОКУМЕНТАЦИЮ СРАЗУ!**

| Что изменилось | Какой файл обновить |
|----------------|--------------------|
| Модель дома (добавил/удалил/изменил) | `CATALOG_GUIDE.md` |
| Структура папок | `ARCHITECTURE.md` |
| CSS переменные/цвета | `DESIGN_SYSTEM.md` |
| Workflow/сервер/токен | `.github/copilot-instructions.md` |
| Любые данные проекта | `mcp_memory_add_observations` |

```bash
# После изменений — коммит с обновлённой документацией
git add ARCHITECTURE.md CATALOG_GUIDE.md DESIGN_SYSTEM.md .github/copilot-instructions.md
git commit -m "docs: обновлена документация после изменений"
```

> ⚠️ **ПРАВИЛО:** Документация = источник истины. Устаревшая документация = баги в будущем.

---

## 📚 ДОКУМЕНТАЦИЯ

- `ARCHITECTURE.md` — Архитектура проекта
- `DESIGN_SYSTEM.md` — Цвета, типографика
- `CATALOG_GUIDE.md` — Гайд по моделям
- `GITHUB_ACTIONS_SETUP.md` — Настройка автодеплоя
- `COPILOT_SETUP_MAC.md` — Настройка Copilot на macOS

---

## 📞 КОНТАКТЫ (константы)

```
Телефон:   +996 222 001 112
WhatsApp:  wa.me/996222001112
Telegram:  @eraconcepthome
Instagram: @eraconcepthome
```

---

## 🛠 СТЕК

React 18 + TypeScript 5 + Vite 5 + Tailwind 3 + shadcn/ui + Framer Motion

---

*При вопросах — сначала читай память (`mcp_memory_read_graph`), потом GitHub Issues (`gh issue list`)*
