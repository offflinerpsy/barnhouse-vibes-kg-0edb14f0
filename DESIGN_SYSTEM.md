# ERA Concept Home — Design System & Styleguide

> Полная карта стилистики, цветов, шрифтов и контента для проекта модульных домов

---

## 🎨 Цветовая палитра

### Основные цвета (Light Mode)

| Название | CSS Variable | HSL | HEX (примерно) | Использование |
|----------|--------------|-----|----------------|---------------|
| **Background** | `--background` | `38 25% 95%` | `#F5F2ED` | Основной фон страницы — тёплый песочный |
| **Foreground** | `--foreground` | `30 10% 15%` | `#2A2622` | Основной текст — тёмный тёплый |
| **Primary (Gold)** | `--primary` | `32 42% 59%` | `#C3996B` | Акцентный золотой — кнопки, ссылки, выделения |
| **Primary Foreground** | `--primary-foreground` | `0 0% 100%` | `#FFFFFF` | Текст на золотом фоне |
| **Secondary** | `--secondary` | `38 20% 90%` | `#EBE6DD` | Вторичный фон — для карточек и секций |
| **Muted** | `--muted` | `38 18% 91%` | `#EAE6DE` | Приглушённый фон |
| **Muted Foreground** | `--muted-foreground` | `30 8% 45%` | `#7A7269` | Вторичный текст — подписи, описания |
| **Card** | `--card` | `38 22% 93%` | `#EFEBE3` | Фон карточек |
| **Border** | `--border` | `35 18% 85%` | `#DDD6C9` | Границы элементов |

### Брендовые цвета (Gold palette)

| Название | CSS Variable | HSL | Использование |
|----------|--------------|-----|---------------|
| **Gold** | `--gold` | `32 42% 59%` | Основной золотой акцент |
| **Gold Light** | `--gold-light` | `35 45% 75%` | Светлый золотой для градиентов |
| **Gold Dark** | `--gold-dark` | `30 40% 45%` | Тёмный золотой для градиентов и hover |
| **Charcoal** | `--charcoal` | `30 15% 20%` | Тёмный угольный — Hero секция, футер |
| **Warm White** | `--warm-white` | `38 25% 95%` | Тёплый белый |
| **Warm Gray** | `--warm-gray` | `35 10% 60%` | Тёплый серый |

### Градиенты

```css
/* Золотой градиент для текста */
.text-gradient-gold {
  background: linear-gradient(to right, 
    hsl(30 40% 45%),   /* gold-dark */
    hsl(32 42% 59%),   /* gold */
    hsl(35 45% 75%)    /* gold-light */
  );
  -webkit-background-clip: text;
  color: transparent;
}

/* Hero градиент (тёмный фон) */
.bg-gradient-hero {
  background: linear-gradient(135deg, 
    hsl(30 15% 20%) 0%,    /* charcoal */
    hsl(30 12% 25%) 100%
  );
}

/* Модальное окно — золотой градиент */
background: linear-gradient(to bottom-right, 
  hsl(var(--gold)), 
  hsl(var(--gold-dark))
);
```

---

## 🔤 Типографика

### Шрифт

**Rising Sun** — основной шрифт проекта (Medium, 500)

```css
font-family: 'Rising Sun', system-ui, sans-serif;
```

### Размеры текста (Desktop 1920px)

| Класс | Размер | Line Height | Weight | Использование |
|-------|--------|-------------|--------|---------------|
| `text-display-xl` | 48px | 1.2 | 500 | Главные заголовки Hero |
| `text-display-lg` | 36px | 1.2 | 500 | Заголовки секций H2 |
| `text-display-md` | 32px | 1.2 | 600 | Подзаголовки |
| `text-heading-lg` | 24px | 1.2 | 500 | H3 заголовки |
| `text-heading-md` | 20px | 1.2 | 500 | H4 заголовки |
| `text-heading-sm` | 18px | 1.2 | 500 | H5 заголовки |
| `text-body-lg` | 18px | 1.2 | 500 | Большой текст |
| `text-body-md` | 16px | 1.2 | 500 | Основной текст |
| `text-body-sm` | 14px | 1.2 | 500 | Мелкий текст |
| `text-caption` | 12px | 1.2 | 500 | Подписи |

### Tailwind классы для заголовков

```html
<!-- Заголовок секции -->
<h2 class="font-display text-3xl md:text-4xl lg:text-5xl font-bold text-foreground">
  Заголовок секции
</h2>

<!-- Подзаголовок/тег над заголовком -->
<span class="text-primary font-medium text-sm uppercase tracking-wider">
  Подзаголовок
</span>

<!-- Компактный заголовок (для FAQ и подобных) -->
<h2 class="font-display text-2xl md:text-3xl lg:text-4xl font-bold text-foreground">
  Компактный заголовок
</h2>
```

---

## 📐 Spacing & Layout

### Отступы секций

```html
<!-- Стандартная секция -->
<section class="py-20 md:py-28">

<!-- Компактная секция -->
<section class="py-14 md:py-20">

<!-- Hero секция -->
<section class="min-h-screen">
```

### Container

```html
<div class="container mx-auto px-4">
  <!-- Контент -->
</div>
```

Ширины контейнера:
- sm: 640px
- md: 768px  
- lg: 1024px
- xl: 1280px
- 2xl: 1400px

### Border Radius

| Размер | Значение | Использование |
|--------|----------|---------------|
| `rounded-sm` | 8px | Мелкие элементы |
| `rounded-md` | 10px | Кнопки, инпуты |
| `rounded-lg` | 12px | Карточки компактные |
| `rounded-xl` | 16px | Карточки стандартные |
| `rounded-2xl` | 20px | Большие карточки, модалки |
| `rounded-3xl` | 24px | Hero элементы |

---

## 🎛 Компоненты

### Кнопки

```html
<!-- Primary (золотая) -->
<button class="bg-primary text-primary-foreground hover:bg-primary/90 
               hover:scale-[1.02] active:scale-[0.98] 
               h-10 px-4 py-2 rounded-md font-semibold">
  Кнопка
</button>

<!-- Outline -->
<button class="border border-input bg-background 
               hover:bg-accent hover:text-accent-foreground
               h-10 px-4 py-2 rounded-md font-semibold">
  Outline
</button>

<!-- Ghost -->
<button class="hover:bg-accent hover:text-accent-foreground
               h-10 px-4 py-2 rounded-md font-semibold">
  Ghost
</button>

<!-- XL размер (для CTA) -->
<button class="h-14 rounded-xl px-8 text-base">
  Большая кнопка
</button>
```

#### iOS-style Inset Shadow Effect

Для кнопок в мобильном каталоге используется эффект впуклости (inset shadow), имитирующий iOS-стиль:

```html
<!-- Кнопка с iOS-эффектом впуклости -->
<motion.button 
  className="btn-inset-press h-14 rounded-2xl bg-primary"
  whileTap={{ scale: 0.97, filter: "brightness(0.85)" }}
>
  Кнопка
</motion.button>
```

**CSS класс `.btn-inset-press`:**
- При нажатии (`:active`) появляется inset shadow через `::before` псевдоэлемент
- Применяется `filter: brightness(0.85)` для затемнения
- Transition 0.15s для плавности
- Совместим с iOS Safari и Android Chrome

**Framer Motion анимация:**
- `whileTap={{ scale: 0.92-0.97, filter: "brightness(0.85)" }}`
- Создаёт эффект "нажатия" с визуальной обратной связью
- Используется для кнопок "Позвонить" и "Сообщение" в футере CatalogAppView

### Карточки

```html
<!-- Стандартная карточка -->
<div class="bg-card rounded-xl px-6 py-5 border border-border">
  <!-- Контент -->
</div>

<!-- Компактная карточка -->
<div class="bg-card rounded-lg px-4 py-3 border border-border">
  <!-- Контент -->
</div>

<!-- Карточка с тенью -->
<div class="bg-card rounded-2xl p-6 border border-border shadow-lg">
  <!-- Контент -->
</div>
```

### Accordion (FAQ)

```html
<div class="bg-card rounded-lg px-4 border border-border">
  <button class="text-left text-sm font-display font-semibold 
                 hover:text-primary py-3 w-full">
    Вопрос
  </button>
  <div class="text-muted-foreground text-sm pb-3 leading-relaxed">
    Ответ
  </div>
</div>
```

---

## ✨ Анимации

### Framer Motion варианты

```tsx
// Контейнер с stagger эффектом
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.08,
      delayChildren: 0.2,
    },
  },
};

// Элементы списка
const itemVariants = {
  hidden: { opacity: 0, y: 30 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.5 },
  },
};
```

### Scroll-triggered анимации

```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true, margin: "-100px" }}
  transition={{ duration: 0.5 }}
>
  Контент
</motion.div>
```

### Keyframes (CSS)

```css
/* Fade Up */
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Fade In */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* Accordion */
@keyframes accordion-down {
  from { height: 0; }
  to { height: var(--radix-accordion-content-height); }
}
```

---

## 📝 Контент FAQ блока

### Заголовок секции

- **Тег:** `Ответы на вопросы`
- **Заголовок:** `Частые вопросы`

### Вопросы и ответы

#### 1. Сроки производства
**Q:** Сколько времени занимает производство модульного дома?

**A:** Срок производства зависит от сложности проекта и составляет от 2 до 8 недель. Мини-дома (до 35м²) изготавливаются за 2-3 недели, стандартные проекты — 4-6 недель, премиум-класс — до 8 недель. Монтаж на участке занимает всего 2-5 дней.

---

#### 2. Изменение планировки
**Q:** Можно ли изменить планировку выбранного проекта?

**A:** Да, мы адаптируем любой проект под ваши пожелания. Можно изменить расположение комнат, добавить или убрать окна, изменить отделку. Индивидуальный проект с нуля также возможен — наши архитекторы разработают дом специально для вас.

---

#### 3. Материалы
**Q:** Какие материалы используются при строительстве?

**A:** Мы используем только сертифицированные материалы: каркас из сухой строганной древесины или ЛСТК, утеплитель — минеральная вата или PIR-плиты, фасад — планкен, HPL-панели или штукатурка. Все материалы экологичны и безопасны для здоровья.

---

#### 4. Доставка и монтаж
**Q:** Как осуществляется доставка и монтаж?

**A:** Модули доставляются на специальном транспорте. Мы работаем по всему Кыргызстану — стоимость доставки зависит от расстояния. Монтаж выполняет наша бригада с использованием автокрана. Сборка дома занимает 2-5 дней в зависимости от проекта.

---

#### 5. Гарантия
**Q:** Какая гарантия предоставляется на дом?

**A:** Мы даём гарантию 5 лет на несущие конструкции и 2 года на инженерные системы и отделку. При соблюдении правил эксплуатации срок службы модульного дома составляет более 50 лет.

---

#### 6. Фундамент
**Q:** Нужен ли фундамент для модульного дома?

**A:** Да, для установки дома необходим фундамент. В большинстве случаев подходят винтовые сваи — это быстрый и экономичный вариант. Также можно использовать ленточный или плитный фундамент. Мы поможем выбрать оптимальный вариант для вашего участка.

---

#### 7. Круглогодичное проживание
**Q:** Можно ли жить в модульном доме круглый год?

**A:** Безусловно! Наши дома рассчитаны на круглогодичное проживание в климате Кыргызстана. Толщина утепления стен — от 150мм, крыши — от 200мм. Дома тёплые зимой и прохладные летом, энергоэффективность соответствует современным стандартам.

---

## 🖼 Стилистика других секций (для референса)

### Hero
- Тёмный фон `bg-charcoal`
- Золотой акцент для тегов и кнопок
- Параллакс эффекты при скролле
- Ken Burns эффект для слайдера

### About
- Фон `bg-secondary/30`
- Двухколоночный layout (изображение + текст)
- Плашка с цифрой "50+" на золотом фоне
- Список с иконками CheckCircle

### Catalog
- Фильтры на кнопках с `rounded-full`
- Карточки домов с бейджами
- Модальное окно с золотым градиентом

### Contact Modal
- Градиентный фон `from-[hsl(var(--gold))] to-[hsl(var(--gold-dark))]`
- Белые инпуты на золотом фоне
- Декоративные blur-элементы

---

## 📱 Breakpoints

| Префикс | Ширина | Использование |
|---------|--------|---------------|
| (none) | < 640px | Mobile |
| `sm:` | ≥ 640px | Small tablets |
| `md:` | ≥ 768px | Tablets |
| `lg:` | ≥ 1024px | Desktop |
| `xl:` | ≥ 1280px | Large desktop |
| `2xl:` | ≥ 1400px | Extra large |

---

## 🔧 Утилиты

```html
<!-- Золотой градиент текста -->
<span class="text-gradient-gold">Текст</span>

<!-- Шрифт Rising Sun -->
<span class="font-rising">Текст</span>

<!-- Fade анимации -->
<div class="animate-fade-up">...</div>
<div class="animate-fade-in">...</div>
```
