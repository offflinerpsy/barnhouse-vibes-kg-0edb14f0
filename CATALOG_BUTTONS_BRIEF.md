# 📋 Дизайн-бриф: Кнопки каталога ERA (Mobile)

> Для создания кнопок в редакторе интерфейсов

---

## 🎨 Цвета

| Название | Значение | Использование |
|----------|----------|---------------|
| **Фон каталога** | `hsl(30 15% 20%)` / `#2E2A26` | Тёмный угольный (charcoal) |
| **Акцент (золото)** | `hsl(32 42% 59%)` / `#C3996B` | CTA кнопки, активные состояния |
| **Текст основной** | `#FFFFFF` | Заголовки, важный текст |
| **Текст вторичный** | `rgba(255,255,255,0.8)` | Обычный текст |
| **Текст третичный** | `rgba(255,255,255,0.6)` | Подписи |
| **Текст приглушённый** | `rgba(255,255,255,0.5)` | Мелкие подписи |

---

## 🪟 Glass-морфизм (ГЛАВНЫЙ СТИЛЬ!)

Все кнопки в каталоге используют стеклянный эффект на тёмном фоне:

### Варианты стекла

```css
/* Стандартная glass-кнопка */
.glass-panel {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(40px);
  -webkit-backdrop-filter: blur(40px);
  border: 1px solid rgba(255, 255, 255, 0.12);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

/* Лёгкая glass-кнопка */
.glass-panel-light {
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* Ультра-лёгкая (для инфо-панелей) */
.glass-panel-ultra-light {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(255, 255, 255, 0.06);
}
```

---

## 📐 Размеры и отступы

### Кнопки-иконки

| Размер | Ширина/Высота | Border Radius | Использование |
|--------|---------------|---------------|---------------|
| Малая | 40×40 px | 12px | Закрытие шторок |
| Стандарт | 44×44 px | 14px | Навигация, закрытие |

### Текстовые кнопки

| Тип | Padding | Border Radius | Использование |
|-----|---------|---------------|---------------|
| Pill малый | 12px 6px | 999px (full) | Фильтры, теги |
| Pill средний | 16px 8px | 999px (full) | Счётчики |
| Карточка | 16px 12px | 16px | Действия в карточках |
| CTA большой | 20px 12px | 16px | Главные действия |

### Внутренние отступы

- Gap между иконкой и текстом: **8px**
- Gap между элементами в ряду: **8px**
- Padding контейнера: **12px** (px-3)

---

## 🔤 Типографика

**Шрифт:** Rising Sun (или system-ui как fallback)

| Тип текста | Размер | Weight | Цвет |
|------------|--------|--------|------|
| Заголовок кнопки | 18px | 700 (bold) | white |
| Текст кнопки | 14px | 500 (medium) | white/80 |
| Мелкий текст | 12px | 500 | white/50 |

---

## ✨ Анимации и состояния

### Нажатие (tap/press)

```css
.button:active {
  transform: scale(0.95);
  transition: transform 0.1s ease;
}

/* Или мягче */
.button:active {
  transform: scale(0.98);
}
```

### Пульсация для CTA кнопок

```css
@keyframes pulse-gold {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(195, 153, 107, 0);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(195, 153, 107, 0.2);
  }
}

.cta-button {
  animation: pulse-gold 2s ease-in-out infinite;
  animation-delay: 1.5s;
}
```

### Hover (для десктоп)

```css
.glass-button:hover {
  background: rgba(255, 255, 255, 0.1);
}
```

---

## 🎯 Примеры готовых кнопок

### 1. Кнопка-иконка (навигация влево/вправо)

```
Размер: 44×44 px
Border Radius: 14px
Фон: glass-panel-light
Иконка: 20×20 px, цвет white/80
```

### 2. Кнопка закрытия (крестик)

```
Размер: 44×44 px
Border Radius: 14px
Фон: glass-panel-light
Иконка X: 24×24 px, цвет white
Анимация: появление с rotation от -180° к 0°
```

### 3. Фильтр-пилюля (активная)

```
Padding: 12px 6px
Border Radius: 999px (full)
Фон: #C3996B (primary/gold)
Текст: 14px medium, цвет charcoal (#2E2A26)
```

### 4. Фильтр-пилюля (неактивная)

```
Padding: 12px 6px
Border Radius: 999px (full)
Фон: transparent
Текст: 14px medium, цвет white/70
```

### 5. CTA кнопка "Оставить заявку"

```
Padding: 20px 12px
Border Radius: 16px
Фон: #C3996B (primary/gold)
Иконка: Send, 20×20 px, цвет charcoal
Текст: 14px semibold, цвет charcoal
Анимация: пульсация gold
```

### 6. Glass-кнопка "Каталог моделей"

```
Padding: 16px 12px
Border Radius: 16px
Фон: glass-panel
Иконка: Grid3X3, 16×16 px, цвет primary
Текст: 14px medium, цвет white/80
Доп. элемент: drag-indicator (полоска 40×4 px, white/40, сверху)
```

### 7. Кнопка "Открыть каталог" (на главной)

```
Padding: 32px 16px
Border Radius: 16px
Фон: #C3996B (primary/gold)
Иконка: Grid3X3, 20×20 px, цвет charcoal
Текст: 16px semibold, цвет charcoal
Gap: 12px
Анимация: пульсация с увеличенным радиусом (12px)
Shadow: 0 10px 40px rgba(195, 153, 107, 0.3)
```

---

## 📱 Важно для мобильных

1. **Safe Area** — учитывай вырезы iPhone (notch):
   - Верх: `padding-top: env(safe-area-inset-top)`
   - Низ: `padding-bottom: env(safe-area-inset-bottom)`

2. **Touch Target** — минимум 44×44 px для касания

3. **Viewport Height** — используй `100dvh` вместо `100vh` для iOS Safari

4. **Backdrop Filter** — обязательно добавляй `-webkit-backdrop-filter` для Safari

---

## 🎨 Figma/Sketch экспорт

При экспорте из редактора:
- Формат иконок: SVG
- Stroke width: 2px (для Lucide-style иконок)
- Цвет иконок: currentColor (наследуется)

---

## 📁 Файлы проекта для референса

- `src/components/landing/CatalogAppView.tsx` — мобильный каталог
- `src/index.css` — CSS переменные цветов
- `tailwind.config.ts` — конфиг Tailwind
- `DESIGN_SYSTEM.md` — полная дизайн-система
