# 🔑 Настройка GitHub доступа для AI-агентов

Этот гайд для AI-агентов которые будут работать с репозиторием.

---

## ⚠️ ВАЖНО: Токен НЕ хранится в репозитории!

GitHub автоматически отзывает токены если видит их в коммитах.  
Каждый агент должен получить токен от @offflinerpsy и настроить локально.

---

## 🚀 Быстрая настройка (для AI-агента в VS Code)

### 1. Клонируй репозиторий

```bash
git clone https://github.com/offflinerpsy/barnhouse-vibes-kg-0edb14f0.git
cd barnhouse-vibes-kg-0edb14f0
```

### 2. Установи зависимости

```bash
npm install
```

### 3. Настрой GitHub токен

**Получи токен от @offflinerpsy**, затем:

```powershell
# Windows PowerShell — сохрани токен в файл
Set-Content -Path "$env:USERPROFILE\.github_token" -Value "ТОКЕН_СЮДА"

# Проверь что сохранился
Get-Content "$env:USERPROFILE\.github_token"
```

```bash
# Linux/Mac — сохрани токен в файл
echo "ТОКЕН_СЮДА" > ~/.github_token
chmod 600 ~/.github_token
```

### 4. Перед git операциями загружай токен

```powershell
# Windows PowerShell
$env:GH_TOKEN = Get-Content "$env:USERPROFILE\.github_token"
```

```bash
# Linux/Mac
export GH_TOKEN=$(cat ~/.github_token)
```

### 5. Проверь что работает

```bash
npm run dev          # Запусти dev сервер (localhost:8080)
npm run build        # Собери билд
git status           # Проверь git
```

---

## 📋 Workflow для AI-агента

1. **Прочитай Issue** на GitHub
2. **Создай ветку:**
   ```bash
   git checkout main
   git pull origin main
   git checkout -b fix/issue-N-описание
   ```
3. **Сделай фикс**
4. **Проверь билд:**
   ```bash
   npm run build
   ```
5. **Коммит и Push:**
   ```bash
   git add <файлы>
   git commit -m "fix(scope): описание

   Closes #N"
   git push origin fix/issue-N-описание
   ```
6. **Напиши комментарий в Issue** что решение готово
7. **Жди ревью от @offflinerpsy**

---

## 🔐 Получение токена

Напиши @offflinerpsy в Telegram или создай Issue с запросом доступа.

Токен будет с ограниченными правами:
- ✅ Push в ветки
- ✅ Создание PR
- ✅ Комментарии в Issues
- ❌ Merge в main (только @offflinerpsy)
- ❌ Настройки репозитория
